-content_for :navbar_search do
  = search_form_for [:admin, q], class: 'form-inline 'do |f|
    = f.input ApplicationHelper::APPLICANT_QUERY, label: false, as: :search, placeholder: t('admin.applicants.index_form.query_placeholder')